[Unit]
Description=Clear first boot marker after system reaches multi-user
After=multi-user.target

[Service]
Type=oneshot
ExecStart=/bin/rm -f /etc/firstboot-pending

[Install]
WantedBy=multi-user.target
